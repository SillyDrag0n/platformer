[gd_resource type="GradientTexture2D" load_steps=2 format=3]

; The falloff every lamp in the game is shaped by - a soft round pool, brightest at the flame and
; gone by the edge. Shared rather than one per lamp so a lantern and a street lamp differ only in
; how far they reach and how warm they are, not in the shape of the light.

[sub_resource type="Gradient" id="Gradient_falloff"]
offsets = PackedFloat32Array(0, 0.35, 1)
colors = PackedColorArray(1, 1, 1, 1, 1, 1, 1, 0.45, 1, 1, 1, 0)

[resource]
gradient = SubResource("Gradient_falloff")
width = 256
height = 256
fill = 1
fill_from = Vector2(0.5, 0.5)
fill_to = Vector2(1, 0.5)
