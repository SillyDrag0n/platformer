class_name UtilityItemData
extends ItemData

# Shown in the HUD utility display instead of icon once the owned quantity hits zero (e.g. an
# emptied flask). Optional - leave unset to just keep showing icon at zero, as dynamite does.
@export var empty_icon : Texture2D
